Use case: infographic-diagram
Asset type: Sage technical architecture diagram for README and resume.
Create a 16:9 landscape, 2048x1152, white-background system architecture diagram with a clear title exactly "Sage Agent Harness × Agentic RAG". Use four large horizontal lanes with big readable typography, strong alignment, thin arrows, generous whitespace, and no tiny text.

Chinese text is the priority: render every Chinese label exactly and crisply, without blur, pseudo-characters, or decorative handwriting. Use only the labels listed below; do not add any other words.

Lane 1 heading exactly "输入与控制面". Flow labels exactly: "用户请求" -> "任务意图" -> "学习意图" -> "检索门禁" -> "TurnContextPlan". Add one short note exactly "意图只收窄候选，不授予权限".

Lane 2 heading exactly "Harness 运行编排". Flow labels exactly: "Harness Graph" -> "Model / Tool Loop" -> "Task DAG / Subagent" -> "Checkpoint" -> "Resume". Add three large labels exactly: "Timeline", "Artifact", "lease / fencing".

Lane 3 heading exactly "PostgreSQL Agentic RAG". Flow labels exactly: "SQLite canonical truth" -> "PostgreSQL search projection" -> "GIN + ts_rank_cd sparse" and "pgvector exact cosine dense" -> "RRF fusion" -> "EvidenceBundle" -> "Claim Sufficiency". From Claim Sufficiency show three clear branches with exact labels: "回答", "Research child", "诚实拒答".

Lane 4 heading exactly "安全与评测护栏". Security flow labels exactly: "Schema / Path" -> "Permission" -> "Policy" -> "Approval" -> "Container Sandbox". Evaluation flow labels exactly: "Intent" -> "Retrieval" -> "Claim" -> "Generation" -> "Recovery" -> "Provider / P95".

Use restrained navy, teal, amber, red, and purple accents. No gradients, no 3D, no watermark, no logo, no extra legend, no cropped nodes, no overlapping text. Make all labels at least 24px equivalent and readable at documentation width.
